pipeline {
    agent {
        label 'vagrant'
    }

    stages {
        stage('Build') {
            steps {
                echo 'Building the project...'
                // Example build step
                sh 'whoami'
            }
        }
    }
}